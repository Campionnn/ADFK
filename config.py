# Roblox Account Manger (RAM) Settings
# Must enable the following settings in RAM: Multi Roblox, Enable Web Server, Allow LaunchAccount Method
# Webserver Port for RAM ex: 8080
port = 0000
# Webserver Password for RAM. Leave blank if you didn't set one ex: "12345678890abcdef
password = ""
# list of usernames in RAM ex: ["username1", "username2"] first username in list is main account
usernames = []
# code to private server ex: "12345678901234567890123456789012"
# if code is not all numbers, open private server link in a browser and copy the code from the link once it changes
private_server_code = ""

# Screen Coordinates
# (x, y) coordinates of the intro close button (make sure it is the white part of the X)
intro_close_coords = (0, 0)
# (x, y) coordinates of the fast travel button
fast_travel_coords = (0, 0)
# (x, y) coordinates of the afk fast travel button (for now use the trading button because devs forgot to move afk fast travel location)
afk_coords = (0, 0)
# (x, y) coordinates of the leaderboard fast travel button
leaderboard_coords = (0, 0)
# (x, y) coordinates of the story fast travel button
story_coords = (0, 0)
# (x, y) coordinates of the teleport after clicking afk
teleport_coords = (0, 0)
# (x, y) coordinates of the leave & claim rewards button in afk area
leave_afk_coords = (0, 0)
# (x, y) coordinates of the claim button in afk area after clicking leave
claim_afk_coords = (0, 0)

# (x, y) coordinates of the world you want to farm (Windmill Village)
world_coords = (0, 0)
# (x, y) coordinates of the infinite mode button
infinite_coords = (0, 0)
# (x, y) coordinates of the confirm button
confirm_coords = (0, 0)
# (x, y) coordinates of the start button
start_coords = (0, 0)